{
    "builds": [{
    "use": "@vercel/python",
    "config":{ "maxLambdasize": "15lb", "runtime": "python3.9" }
    }],
    "routes" : [
        {
            "Src": "/(.*)",
            "dest":"WebApp/wsgi.py"
        }
    ]
}
